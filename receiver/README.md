```
=================================================
  Receiver
  CO.RECV-001 · the-deck-host/receiver · felt stage
=================================================
```

**Host island** under `ALICE_BOX/the-deck-host/receiver/` — not a My Pocket Things bay.  
(Moved here from `ALICE_BOX/receiver/` — stands as half-state multi-object stage.)

**One-note rebirth seed** is separate: `my-pocket-things/pocket-desktop/` · **CO.MYPT-004-DESK**.

House-shaped ROM: one window, green felt, things you **spawn** onto the desk and keep open together.

Not “icons that launch other Deck Hosts.” Objects live **on the felt** inside this stage.

**Launch:** `prod\run-receiver.bat` — maximizes to origin (0,0).  
True edge-to-edge: set `RECEIVER_FULLSCREEN=1` then launch (Esc may need host chrome to exit).

### Slice 1.2 (now)

**Two object kinds on the felt:**

| Spawn | Closed | Open | Disk |
|--------|--------|------|------|
| **Loose leaf** | taped label only | one writing sheet | `safe_box/leaves/*.leaf.md` (legacy scraps still load) |
| **Notebook** | cloth book + leaf count | TOC · ←/→ scrub · + page · multi-leaf | `safe_box/books/*.bok` |
| **From Pocket Journal / Notebook** | (imports a copy) | opens immediately as real notebook | copy into Receiver `books/` |

- **Save** / Ctrl+S on both (toast shows chars or page count)
- Re-launch restores leaves **and** notebooks from disk
- **Next:** slip a leaf into a journal (merge)
- Dynamic page design (paper styles / soft markdown) — talk then build; no fake WYSIWYG toggle required yet

### Run

```bat
prod\run-receiver.bat
```

Browser: `prod\run-receiver-browser.bat` → http://127.0.0.1:43200/

### Not yet (and not pretending)

- TOC / page scrub / multi-page markdown like **My Pocket Notebook**  
- That app stays the real notebook ROM; Receiver scraps are **play objects on the felt**  
- Spawn other object kinds · cross-ROM drag

### Name

**Receiver** — receives what you spawn. Not an OS story. A host-shaped room.

### Agents after compact

Read **`docs/CUTENESS-AND-CRAFT.md`** and  
`!q-and-alice-only/histories/where-left-off-2026-08-03-compaction-receiver.md`  
before touching the felt.

Cuteness is load-bearing. Leaves = notecards + terminal edit ritual.  
No Save on paper. No caret-on-pretty overlay. Skin tokens later.
